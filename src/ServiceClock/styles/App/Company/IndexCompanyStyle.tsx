import { StyleSheet } from "react-native";
import geral from "../../Common/Geral";
import { Theme } from '../../../provider/ThemeProvider';

export const createIndexCompanyStyle = (theme: Theme) =>
  StyleSheet.create({
    
  });
