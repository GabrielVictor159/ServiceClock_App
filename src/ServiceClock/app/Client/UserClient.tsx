import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import DefaultLayout from '../../components/DefaultLayout';

const UserClient: React.FC = () => {
    return (
        <>
        <DefaultLayout>
            <Text>UserClient</Text>
        </DefaultLayout>
        </>
    );
};

export default UserClient;